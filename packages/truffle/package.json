{
  "name": "staking-dapp-contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "truffle build",
    "deploy": "rm -rf build && truffle deploy",
    "lint": "run-p lint:js lint:sol",
    "lint:sol": "solium -d contracts/",
    "lint:js": "eslint test/**/*.js",
    "test": "truffle test --network test"
  },
  "author": "WeTrustPlatform",
  "license": "GPL-3.0",
  "dependencies": {
    "web3": "1.0.0-beta.37"
  }
}
